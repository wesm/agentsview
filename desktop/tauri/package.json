{
  "name": "agentsview-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare-sidecar": "./scripts/prepare-sidecar.sh",
    "tauri:dev": "npm run prepare-sidecar && tauri dev",
    "tauri:build": "npm run prepare-sidecar && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
